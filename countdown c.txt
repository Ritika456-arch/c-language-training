#include<stdio.h>

    void countdown(int n)
{
    if (n == 0)
    {
        printf("0\n");
        return;
    }

    printf("%d\n", n);
    countdown(n - 1);
}

int main()
{
    printf("countdown from 10:\n ");
    countdown(10);

    return 0;
}
