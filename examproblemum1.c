#include <stdio.h>

void rearrangeArray(int nums[], int n)
{
    int temp[n];
    int r = 0;

    for (int i = 0; i < n; i++)
    if (arr[i]<0)
    temp[r++] = arr[i];
    
    for (int i = 0; i < n; i++)
     if(arr[i]<0)
     temp[r++] = arr[i];
     for(int i = 0 ; i< n; i++) 
     arr[i] = temp[i]
    }
     
    int main()
    {
    int arr() = {1, -2, 3, -4, 5, -6}
    int n = sizeof(arr)/sizeof(arr)[0];

    rearrangearray(arr,n);

    for(int i = 0;i < n ; j++);
    printf("%d",arr[i]);

    return 0;
}
